apiVersion: v1
kind: Service
metadata:
  name: wisecow-service
  labels:
    app: wisecow
spec:
  type: ClusterIP
  selector:
    app: wisecow
  ports:
    - protocol: TCP
      port: {{ .Values.wisecow.port }}
      targetPort: {{ .Values.wisecow.targetPort }}