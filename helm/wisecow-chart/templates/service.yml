apiVersion: v1
kind: Service
metadata:
  name: wisecow-service
  labels:
    app: wisecow
spec:
  selector:
    app: wisecow
  ports:
    - protocol: TCP
      name: http
      port: {{ .Values.wisecow.service.port }}
      targetPort: {{ .Values.wisecow.service.port }}